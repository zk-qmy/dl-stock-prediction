import pandas as pd
import os
from utils.db_connection import get_db_engine

# Database connection
engine = get_db_engine()

# Folder path
folder_path = 'data'  # Update this if needed

# Data insertion logic remains the same
